<% content_for(:title, "Sections | Learn to Program") %>

<h1>Sections</h1>

<ul>
  <% @sections.each do |section| %>
  <li><%= link_to section.name, section_path(section) %></li>
  <% end %>
</ul>

<p>
  <%= link_to "Create a New Section", new_section_path(@section) %>
</p>
